{
	"name": "@arbiti/core",
	"type": "module",
	"version": "0.1.1",
	"description": "Core assets and functions for @arbiti packages",
	"main": "dist/index.js",
	"files": [
		"dist",
		"package.json",
		"tsconfig.json"
	],
	"scripts": {
		"build": "rimraf dist && tsc && ARBITI_ENV=production node ../../scripts/setEnv.mjs dist/env.js",
		"build:dev": "rimraf dist && tsc && node ../../scripts/setEnv.mjs dist/env.js",
		"dev": "tsc -w"
	},
	"keywords": [
		"arbiti",
		"core",
		"service-worker"
	],
	"author": "Arbiti.com",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.8.2",
		"rimraf": "^6.0.1",
		"typescript": "^5.6.3"
	}
}
