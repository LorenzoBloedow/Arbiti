// Create setup CLI function that logs in user, detects the environment and downloads the appropriate packages
// (for example, node vs nextjs, svelte, etc), creates worker files, adds env variables and secrets, and writes setup code
// and creates an api endpoint or server action, and other things
